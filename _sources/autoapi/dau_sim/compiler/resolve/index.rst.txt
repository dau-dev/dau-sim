dau_sim.compiler.resolve
========================

.. py:module:: dau_sim.compiler.resolve

.. autoapi-nested-parse::

   Wire resolution for multi-driver nets.

   When multiple combinational blocks drive the same signal, we must merge
   the driven values according to the net's resolution semantics.

   Resolution rules (per IEEE 1800-2017):
     - WIRE / TRI: Z resolves to the non-Z driver; two non-Z drivers → X
     - WAND: driven values ANDed together (Z treated as all-1)
     - WOR: driven values ORed together (Z treated as all-0)



Functions
---------

.. autoapisummary::

   dau_sim.compiler.resolve.resolve_drivers
   dau_sim.compiler.resolve._resolve_tri
   dau_sim.compiler.resolve._resolve_wand
   dau_sim.compiler.resolve._resolve_wor


Module Contents
---------------

.. py:function:: resolve_drivers(drivers: list[dau_sim.ir.types.FourState], kind: dau_sim.ir.types.NetKind, shape: dau_sim.ir.types.Shape) -> dau_sim.ir.types.FourState

   Resolve multiple driver values into a single net value.

   :param drivers: One or more FourState values, one per driver.
   :param kind: The net resolution semantics.
   :param shape: The shape of the resulting signal.

   :returns: Resolved FourState value.


.. py:function:: _resolve_tri(drivers: list[dau_sim.ir.types.FourState], shape: dau_sim.ir.types.Shape) -> dau_sim.ir.types.FourState

   Tri-state resolution: Z yields to non-Z; conflicting non-Z → X.

   Per-bit truth table (d1, d2):
     0, Z → 0    Z, 0 → 0
     1, Z → 1    Z, 1 → 1
     0, 1 → X    1, 0 → X
     X, * → X    *, X → X
     Z, Z → Z


.. py:function:: _resolve_wand(drivers: list[dau_sim.ir.types.FourState], shape: dau_sim.ir.types.Shape) -> dau_sim.ir.types.FourState

   Wired-AND: AND all drivers together. Z treated as all-1.


.. py:function:: _resolve_wor(drivers: list[dau_sim.ir.types.FourState], shape: dau_sim.ir.types.Shape) -> dau_sim.ir.types.FourState

   Wired-OR: OR all drivers together. Z treated as all-0.


