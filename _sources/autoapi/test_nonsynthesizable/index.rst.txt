test_nonsynthesizable
=====================

.. py:module:: test_nonsynthesizable

.. autoapi-nested-parse::

   Tests for non-synthesizable constructs.

   Covers: initial blocks, $display (Print), assertions, $finish (Finish),
   delay statements, $random (SysRandom), $readmemh/$readmemb (ReadMem),
   and SimulationFinish exception.



Classes
-------

.. autoapisummary::

   test_nonsynthesizable.TestInitBlock
   test_nonsynthesizable.TestPrintStmt
   test_nonsynthesizable.TestAssertStmt
   test_nonsynthesizable.TestFinishStmt
   test_nonsynthesizable.TestDelayStmt
   test_nonsynthesizable.TestIntegration
   test_nonsynthesizable.TestSysRandom
   test_nonsynthesizable.TestReadMem


Functions
---------

.. autoapisummary::

   test_nonsynthesizable._make_signal
   test_nonsynthesizable._make_port


Module Contents
---------------

.. py:function:: _make_signal(name, width=8, init=0)

.. py:function:: _make_port(name, width=8, direction=PortDirection.INPUT, init=0)

.. py:class:: TestInitBlock(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   A class whose instances are single test cases.

   By default, the test code itself should be placed in a method named
   'runTest'.

   If the fixture may be used for many test cases, create as
   many test methods as are needed. When instantiating such a TestCase
   subclass, specify in the constructor arguments the name of the test method
   that the instance is to execute.

   Test authors should subclass TestCase for their own tests. Construction
   and deconstruction of the test's environment ('fixture') can be
   implemented by overriding the 'setUp' and 'tearDown' methods respectively.

   If it is necessary to override the __init__ method, the base class
   __init__ method must always be called. It is important that subclasses
   should not change the signature of their __init__ method, since instances
   of the classes are instantiated automatically by parts of the framework
   in order to be run.

   When subclassing TestCase, you can set these attributes:
   * failureException: determines which exception will be raised when
       the instance's assertion methods fail; test methods raising this
       exception will be deemed to have 'failed' rather than 'errored'.
   * longMessage: determines whether long messages (including repr of
       objects used in assert methods) will be printed on failure in *addition*
       to any explicit message passed.
   * maxDiff: sets the maximum length of a diff in failure messages
       by assert methods using difflib. It is looked up as an instance
       attribute so can be configured by individual tests if required.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: test_init_block_sets_value()

      Initial block assigns to signal before simulation starts.



   .. py:method:: test_init_block_multiple_assigns()

      Multiple assignments in an init block.



   .. py:method:: test_init_block_overrides_port_init()

      Init block values take priority over port init values.



   .. py:method:: test_multiple_init_blocks()

      Multiple init blocks execute in order.



   .. py:method:: test_init_block_with_sequential()

      Init blocks work with sequential modules.



.. py:class:: TestPrintStmt(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   A class whose instances are single test cases.

   By default, the test code itself should be placed in a method named
   'runTest'.

   If the fixture may be used for many test cases, create as
   many test methods as are needed. When instantiating such a TestCase
   subclass, specify in the constructor arguments the name of the test method
   that the instance is to execute.

   Test authors should subclass TestCase for their own tests. Construction
   and deconstruction of the test's environment ('fixture') can be
   implemented by overriding the 'setUp' and 'tearDown' methods respectively.

   If it is necessary to override the __init__ method, the base class
   __init__ method must always be called. It is important that subclasses
   should not change the signature of their __init__ method, since instances
   of the classes are instantiated automatically by parts of the framework
   in order to be run.

   When subclassing TestCase, you can set these attributes:
   * failureException: determines which exception will be raised when
       the instance's assertion methods fail; test methods raising this
       exception will be deemed to have 'failed' rather than 'errored'.
   * longMessage: determines whether long messages (including repr of
       objects used in assert methods) will be printed on failure in *addition*
       to any explicit message passed.
   * maxDiff: sets the maximum length of a diff in failure messages
       by assert methods using difflib. It is looked up as an instance
       attribute so can be configured by individual tests if required.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: test_display_basic()

      Print stmt outputs to stdout during simulation.



   .. py:method:: test_display_format_multiple_args()

      Print with multiple arguments.



   .. py:method:: test_display_in_init_block()

      Print in init block executes at time 0.



.. py:class:: TestAssertStmt(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   A class whose instances are single test cases.

   By default, the test code itself should be placed in a method named
   'runTest'.

   If the fixture may be used for many test cases, create as
   many test methods as are needed. When instantiating such a TestCase
   subclass, specify in the constructor arguments the name of the test method
   that the instance is to execute.

   Test authors should subclass TestCase for their own tests. Construction
   and deconstruction of the test's environment ('fixture') can be
   implemented by overriding the 'setUp' and 'tearDown' methods respectively.

   If it is necessary to override the __init__ method, the base class
   __init__ method must always be called. It is important that subclasses
   should not change the signature of their __init__ method, since instances
   of the classes are instantiated automatically by parts of the framework
   in order to be run.

   When subclassing TestCase, you can set these attributes:
   * failureException: determines which exception will be raised when
       the instance's assertion methods fail; test methods raising this
       exception will be deemed to have 'failed' rather than 'errored'.
   * longMessage: determines whether long messages (including repr of
       objects used in assert methods) will be printed on failure in *addition*
       to any explicit message passed.
   * maxDiff: sets the maximum length of a diff in failure messages
       by assert methods using difflib. It is looked up as an instance
       attribute so can be configured by individual tests if required.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: test_assert_pass()

      Assertion passes when condition is true.



   .. py:method:: test_assert_fail()

      Assertion raises when condition is false.



   .. py:method:: test_assert_default_message()

      Assert with no message uses default.



   .. py:method:: test_assert_in_init_block()

      Assert in init block fires at time 0.



.. py:class:: TestFinishStmt(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   A class whose instances are single test cases.

   By default, the test code itself should be placed in a method named
   'runTest'.

   If the fixture may be used for many test cases, create as
   many test methods as are needed. When instantiating such a TestCase
   subclass, specify in the constructor arguments the name of the test method
   that the instance is to execute.

   Test authors should subclass TestCase for their own tests. Construction
   and deconstruction of the test's environment ('fixture') can be
   implemented by overriding the 'setUp' and 'tearDown' methods respectively.

   If it is necessary to override the __init__ method, the base class
   __init__ method must always be called. It is important that subclasses
   should not change the signature of their __init__ method, since instances
   of the classes are instantiated automatically by parts of the framework
   in order to be run.

   When subclassing TestCase, you can set these attributes:
   * failureException: determines which exception will be raised when
       the instance's assertion methods fail; test methods raising this
       exception will be deemed to have 'failed' rather than 'errored'.
   * longMessage: determines whether long messages (including repr of
       objects used in assert methods) will be printed on failure in *addition*
       to any explicit message passed.
   * maxDiff: sets the maximum length of a diff in failure messages
       by assert methods using difflib. It is looked up as an instance
       attribute so can be configured by individual tests if required.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: test_finish_in_init_block()

      $finish in init block returns single-point traces.



   .. py:method:: test_finish_in_comb_block()

      $finish in comb block stops simulation after first eval.



   .. py:method:: test_finish_conditional()

      $finish only fires when count reaches threshold.



   .. py:method:: test_simulation_finish_exception()

      SimulationFinish stores exit code.



.. py:class:: TestDelayStmt(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   A class whose instances are single test cases.

   By default, the test code itself should be placed in a method named
   'runTest'.

   If the fixture may be used for many test cases, create as
   many test methods as are needed. When instantiating such a TestCase
   subclass, specify in the constructor arguments the name of the test method
   that the instance is to execute.

   Test authors should subclass TestCase for their own tests. Construction
   and deconstruction of the test's environment ('fixture') can be
   implemented by overriding the 'setUp' and 'tearDown' methods respectively.

   If it is necessary to override the __init__ method, the base class
   __init__ method must always be called. It is important that subclasses
   should not change the signature of their __init__ method, since instances
   of the classes are instantiated automatically by parts of the framework
   in order to be run.

   When subclassing TestCase, you can set these attributes:
   * failureException: determines which exception will be raised when
       the instance's assertion methods fail; test methods raising this
       exception will be deemed to have 'failed' rather than 'errored'.
   * longMessage: determines whether long messages (including repr of
       objects used in assert methods) will be printed on failure in *addition*
       to any explicit message passed.
   * maxDiff: sets the maximum length of a diff in failure messages
       by assert methods using difflib. It is looked up as an instance
       attribute so can be configured by individual tests if required.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: test_delay_ir_node()

      Delay IR node stores ticks.



   .. py:method:: test_delay_in_init_block()

      Delay in init block is no-op (doesn't crash).



   .. py:method:: test_delay_in_comb_block()

      Delay in comb block is ignored.



.. py:class:: TestIntegration(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   A class whose instances are single test cases.

   By default, the test code itself should be placed in a method named
   'runTest'.

   If the fixture may be used for many test cases, create as
   many test methods as are needed. When instantiating such a TestCase
   subclass, specify in the constructor arguments the name of the test method
   that the instance is to execute.

   Test authors should subclass TestCase for their own tests. Construction
   and deconstruction of the test's environment ('fixture') can be
   implemented by overriding the 'setUp' and 'tearDown' methods respectively.

   If it is necessary to override the __init__ method, the base class
   __init__ method must always be called. It is important that subclasses
   should not change the signature of their __init__ method, since instances
   of the classes are instantiated automatically by parts of the framework
   in order to be run.

   When subclassing TestCase, you can set these attributes:
   * failureException: determines which exception will be raised when
       the instance's assertion methods fail; test methods raising this
       exception will be deemed to have 'failed' rather than 'errored'.
   * longMessage: determines whether long messages (including repr of
       objects used in assert methods) will be printed on failure in *addition*
       to any explicit message passed.
   * maxDiff: sets the maximum length of a diff in failure messages
       by assert methods using difflib. It is looked up as an instance
       attribute so can be configured by individual tests if required.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: test_init_print_assert()

      Full flow: init sets values, print displays them, assert checks.



   .. py:method:: test_sequential_init_block()

      Init block with a sequential counter: count starts at custom value.



.. py:class:: TestSysRandom(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   Tests for $random system function.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: test_random_returns_value_in_shape()

      $random assigns a value that fits within the output shape.



   .. py:method:: test_random_produces_varying_values()

      Multiple $random evaluations produce different values (high probability).



   .. py:method:: test_random_with_seed_is_deterministic()

      $random(seed) produces the same sequence across runs.



   .. py:method:: test_random_in_init_block()

      $random works inside initial blocks.



   .. py:method:: test_random_signed_shape()

      $random with signed shape can produce negative values (over many runs).



.. py:class:: TestReadMem(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   Tests for $readmemh/$readmemb system tasks.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: _make_mem_module(init_stmts)

      Helper: create a module with a 8x8 memory and init blocks.



   .. py:method:: test_readmemh_basic()

      $readmemh loads hex values into memory.



   .. py:method:: test_readmemb_basic()

      $readmemb loads binary values into memory.



   .. py:method:: test_readmemh_with_comments()

      $readmemh ignores // comments and blank lines.



   .. py:method:: test_readmemh_with_address()

      $readmemh supports @address directives.



   .. py:method:: test_readmemh_with_start_end_addr()

      $readmemh with start/end address limits the range.



   .. py:method:: test_readmemh_multiple_tokens_per_line()

      $readmemh handles multiple space-separated values per line.



   .. py:method:: test_readmem_bad_memory_name()

      $readmemh with unknown memory name raises RuntimeError.



