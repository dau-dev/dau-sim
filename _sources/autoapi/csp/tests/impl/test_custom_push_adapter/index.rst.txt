csp.tests.impl.test_custom_push_adapter
=======================================

.. py:module:: csp.tests.impl.test_custom_push_adapter


Attributes
----------

.. autoapisummary::

   csp.tests.impl.test_custom_push_adapter._CallablePushAdapter
   csp.tests.impl.test_custom_push_adapter._CoordinatedPushAdapter


Classes
-------

.. autoapisummary::

   csp.tests.impl.test_custom_push_adapter._CallablePushAdapterImpl
   csp.tests.impl.test_custom_push_adapter._GroupCoordinator
   csp.tests.impl.test_custom_push_adapter._CoordinatedPushAdapterImpl
   csp.tests.impl.test_custom_push_adapter.TestCallablePushAdapter


Functions
---------

.. autoapisummary::

   csp.tests.impl.test_custom_push_adapter.callable_py_push_adapter_def
   csp.tests.impl.test_custom_push_adapter._run_adapter


Module Contents
---------------

.. py:function:: callable_py_push_adapter_def(name, adapterimpl, out_type, manager_type=None, memoize=True, force_memoize=False, **kwargs)

.. py:class:: _CallablePushAdapterImpl(typ, callables, sleep_interval=0.01)

   Bases: :py:obj:`csp.lib._csptestlibimpl.CallablePyPushInputAdapter`


   Adapter impl that pushes callables from a shared list.


   .. py:attribute:: _callables


   .. py:attribute:: _sleep_interval
      :value: 0.01



   .. py:attribute:: _thread
      :value: None



   .. py:attribute:: _running
      :value: False



   .. py:method:: start(starttime, endtime)


   .. py:method:: stop()


   .. py:method:: _run()


.. py:data:: _CallablePushAdapter

.. py:function:: _run_adapter(graph, timeout=5)

   Helper to run a graph in realtime mode with standard settings.


.. py:class:: _GroupCoordinator(nc_callables, lv_callables)

   Coordinates two adapters in the same PushGroup, pushing events via PushBatch
   from a single thread to ensure deterministic ordering.


   .. py:attribute:: nc_callables


   .. py:attribute:: lv_callables


   .. py:attribute:: nc_adapter
      :value: None



   .. py:attribute:: lv_adapter
      :value: None



   .. py:attribute:: _engine
      :value: None



   .. py:attribute:: _thread
      :value: None



   .. py:attribute:: _ready


   .. py:method:: register(adapter, role)


   .. py:method:: start()


   .. py:method:: _run()


   .. py:method:: stop()


.. py:class:: _CoordinatedPushAdapterImpl(typ, coordinator, role)

   Bases: :py:obj:`csp.lib._csptestlibimpl.CallablePyPushInputAdapter`


   Adapter impl that registers with a coordinator instead of pushing events itself.


   .. py:attribute:: _coordinator


   .. py:attribute:: _role


   .. py:method:: start(starttime, endtime)


   .. py:method:: stop()


.. py:data:: _CoordinatedPushAdapter

.. py:class:: TestCallablePushAdapter(methodName='runTest')

   Bases: :py:obj:`unittest.TestCase`


   A class whose instances are single test cases.

   By default, the test code itself should be placed in a method named
   'runTest'.

   If the fixture may be used for many test cases, create as
   many test methods as are needed. When instantiating such a TestCase
   subclass, specify in the constructor arguments the name of the test method
   that the instance is to execute.

   Test authors should subclass TestCase for their own tests. Construction
   and deconstruction of the test's environment ('fixture') can be
   implemented by overriding the 'setUp' and 'tearDown' methods respectively.

   If it is necessary to override the __init__ method, the base class
   __init__ method must always be called. It is important that subclasses
   should not change the signature of their __init__ method, since instances
   of the classes are instantiated automatically by parts of the framework
   in order to be run.

   When subclassing TestCase, you can set these attributes:
   * failureException: determines which exception will be raised when
       the instance's assertion methods fail; test methods raising this
       exception will be deemed to have 'failed' rather than 'errored'.
   * longMessage: determines whether long messages (including repr of
       objects used in assert methods) will be printed on failure in *addition*
       to any explicit message passed.
   * maxDiff: sets the maximum length of a diff in failure messages
       by assert methods using difflib. It is looked up as an instance
       attribute so can be configured by individual tests if required.

   Create an instance of the class that will use the named test
   method when executed. Raises a ValueError if the instance does
   not have a method with the specified name.


   .. py:method:: test_basic_callable_dict()

      Push one callable returning a dict, verify output.



   .. py:method:: test_callable_dict_nested()

      Push a callable returning a nested dict.



   .. py:method:: test_multiple_ticks_dict()

      Push N callables returning dicts, verify all arrive in order.



   .. py:method:: test_stateful_callable_dict()

      Push closures sharing mutable state, verify incrementing values.



   .. py:method:: test_basic_callable_list()

      Push one callable returning a list, verify output.



   .. py:method:: test_multiple_ticks_list()

      Push N callables returning lists, verify all arrive in order.



   .. py:method:: test_basic_callable_object()

      Push one callable returning a tuple (via object type), verify output.



   .. py:method:: test_callable_object_custom_class()

      Push a callable returning a custom class instance.



   .. py:method:: test_multiple_ticks_object()

      Push N callables returning mixed Python objects.



   .. py:method:: test_callable_that_raises()

      Push a callable that raises, verify engine surfaces error.



   .. py:method:: test_reconsume_non_collapsing()

      Push N callables with NON_COLLAPSING mode and no sleep. Ungrouped NC adapters process
      one event per engine cycle, so remaining events are deferred to pending and reconsumed with
      reconsuming=true. Verify all arrive in order and each callable is invoked exactly once.



   .. py:method:: test_reconsume_push_group()

      Two adapters in the same PushGroup (one NC, one LAST_VALUE) pushed via PushBatch.
      The NC event locks the group at the batch boundary, deferring the LV event to pending.
      The LV event is reconsumed on the next cycle with reconsuming=true.
      Verifies isGroupEnd preservation and that each callable is invoked exactly once.



